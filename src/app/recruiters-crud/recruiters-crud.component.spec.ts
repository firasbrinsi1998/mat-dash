/*

import { ComponentFixture, TestBed } from '@angular/core/testing';

import { RecruitersCrudComponent } from './recruiters-crud.component';

describe('RecruitersCrudComponent', () => {
  let component: RecruitersCrudComponent;
  let fixture: ComponentFixture<RecruitersCrudComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ RecruitersCrudComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(RecruitersCrudComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/